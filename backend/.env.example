# Backend environment variables example
# Copy this file to backend/.env and fill real values (DO NOT commit .env with secrets)

# App
PORT=3000
FRONTEND_URL=http://localhost:3000

# JWT / tokens
# Recommended: set real random secrets for production
ACCESS_TOKEN_SECRET=your_access_token_secret_here
REFRESH_TOKEN_SECRET=your_refresh_token_secret_here

# MongoDB (Atlas or local)
# Either MONGODB_URI or MONGO_URI will be used by scripts
MONGODB_URI=mongodb+srv://<user>:<pass>@cluster0.mongodb.net/dbname
# or for local:
# MONGODB_URI=mongodb://localhost:27017/yourdb

# Cloudinary (optional for avatar upload)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret

# Email (Gmail recommended / App Password)
# For Gmail, enable 2FA and create an App Password, then use it here
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM="No Reply <no-reply@example.com>"

# Optional: direct SMTP (e.g., Mailtrap) — uncomment if using
# EMAIL_HOST=smtp.mailtrap.io
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=mailtrap-user
# EMAIL_PASS=mailtrap-pass
